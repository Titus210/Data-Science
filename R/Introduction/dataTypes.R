# locical datatype
var_logical <- TRUE
cat("The data type of variable logic is ", class(var_logical), "\n")

var_numeric <- 2121
cat("The data type of variable numeric is ", class(var_numeric), "\n")

var_integer <- 1322L
cat("The data type of variable integer is ", class(var_integer), "\n")

var_complex <- 3 + 2i
cat("The data type of variable complex is ", class(var_complex), "\n")

var_character <- "Titus Kiplagat"
cat("The data type of variable character is ", class(var_character), "\n")

